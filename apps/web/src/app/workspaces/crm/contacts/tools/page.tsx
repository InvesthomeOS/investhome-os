import { ContactsVerificationTools } from '../_components/contacts-verification-tools';

export default function CrmContactsToolsPage() {
  return (
    <main className="dashboard crm-module-shell" data-testid="crm-contacts-tools-page">
      <ContactsVerificationTools />
    </main>
  );
}
